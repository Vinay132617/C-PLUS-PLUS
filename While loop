#include <iostream>

using namespace std;

int main()
{
    
    /*int num{};
    cout<<"Enter number : ";
    cin>>num;
    
    while(num >= 0){
        cout<<num<<endl;
        --num;
    }
    cout<<"Blastoff!"<<endl;*/
    
    /*int num{};
    cout<<"Enter the number between 1 and 5 : ";
    cin>>num;
    
    while(num >= 5 || num <= 1)
    {   cout<<"Enter the number between 1 and 5 : ";
        cin>>num;
    }
    
    cout<<"Thanks!"<<endl;*/
    
    bool done{false};
    int num{};
    
    while (!done)
    {
        cout<<"Enter the number between 1 and 5 : ";
        cin>>num;    
        
        if(num >= 5 || num <= 1)
            cout<<"out of the range"<<endl;
        else{
            cout<<"Thanks"<<endl;
            done = true;
        }
    }
    
}
